name: Build nova-ADEN (Android Universal)

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y openjdk-17-jdk wget unzip

      - name: Get dependencies
        working-directory: ./nova_aden
        run: flutter pub get

      # CORRECCIÓN: Ignorar infos y warnings para que el análisis pase
      - name: Analyze project
        working-directory: ./nova_aden
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      # CORRECCIÓN: --release SIN --split-per-abi para generar UN SOLO APK UNIVERSAL
      - name: Build Universal APK
        working-directory: ./nova_aden
        run: flutter build apk --release

      - name: Upload Universal APK
        uses: actions/upload-artifact@v4
        with:
          name: nova-aden-universal-apk
          path: nova_aden/build/app/outputs/flutter-apk/*.apk
